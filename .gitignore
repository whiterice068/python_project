# 密钥和环境变量（绝对禁止提交）
.env
*.env

# Python 缓存（垃圾文件）
__pycache__/
*.pyc
*.pyo

# 虚拟环境（太大了，不需要备份到 GitHub）
venv/
env/
.venv/

# IDE 配置（每个人的习惯不同）
.vscode/
.idea/

# 数据文件（太大了，且属于“原材料”，不需要备份）
data/
outputs/
*.csv
*.xlsx

test.py
info.txt
note.py